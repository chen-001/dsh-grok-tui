# @deepseek-ai/* and cordis are unpublished workspace packages provided by
# the active dsh checkout at runtime (resolved via the checkout's tsx hook),
# so they must never be fetched from the npm registry. legacy-peer-deps
# restores npm <7 behaviour: don't auto-install peerDependencies.
legacy-peer-deps=true
